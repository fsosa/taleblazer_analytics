<% extend 'layout.ect' %>

    <% block 'page-wrapper' :%>
        <div id="page-wrapper" class="container-fluid">

            <% include 'page-wrapper-header.ect' %>

                <div class="row">

                    <div class="col-lg-12">
                        <div class="panel panel-default">
                            <div id="datatable-heading" class="panel-heading">
                                Statistics
                            </div>

                            <div class="panel-body">
                                <div class="table-responsive">
                                    <table class="table table-striped table-bordered table-hover" id="dataTable">
                                        <!-- Filled in via JS initialization / This is the default if no data is available on page load -->
                                        <thead>
                                            <tr>
                                                <th>Data</th>
                                            </tr>
                                        </thead>

                                        <tbody>
                                            <tr>
                                                <td></td>
                                            </tr>
                                        </tbody>
                                        
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>

                </div>

        </div>
        <% end %>